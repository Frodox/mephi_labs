#include <special_funcs.h>

#include <iostream>
using namespace std;

void initialize_A (Matrix &A)
{
//    cout << "Inithialize A" << endl;
    if ((A.rows() == 5) && (A.cols() == 5))
    {
        // ... A.setVal(0, 0, 13);
    }
}
